# 三产品统一入口说明

本仓库统一维护以下三个产品的交互原型：

1. 超级智能体
2. 智能体管理后台
3. 记忆管理系统

三个产品使用同一站点地址，通过页面顶部的产品切换按钮进入。产品相互独立，保留各自的用户、对象、权限和功能边界，但不再建设三个互相割裂的原型地址。

## 地址约定

| 产品 | 本地地址 | 产品标识 |
| --- | --- | --- |
| 超级智能体 | `http://localhost:4178/` | `superagent` |
| 智能体管理后台 | `http://localhost:4178/?product=admin` | `admin` |
| 记忆管理系统 | `http://localhost:4178/?product=memory-system` | `memory-system` |

默认根地址始终进入超级智能体。产品切换使用浏览器历史记录，前进、后退能够恢复对应产品。

## 当前实现

- 产品切换器、产品标识和统一路由位于 `site/app/fiscal-workbench.tsx`。
- 公共视觉样式位于 `site/app/visual-refinement.css`。
- 超级智能体已经是完整工作原型。
- 智能体管理后台和记忆管理系统目前已建立产品首页容器；后续构建具体页面时，应直接接入现有产品标识和切换器。

## 后续接入方式

- 管理后台的新页面代码放在 `site/app/products/admin/`。
- 记忆管理系统的新页面代码放在 `site/app/products/memory-system/`。
- 跨产品共用的切换器、路由和外壳放在 `site/app/product-shell/`；后续从现有文件拆分时必须保持产品标识和地址兼容。
- 每个产品可以有自己的侧栏、首页和业务流程，但顶部必须保留同一个产品切换入口。
- 不通过外部跳转、独立端口或另一套发布地址切换产品，也不复制一套无法同步维护的静态页面。

## 修改后的最低检查

1. 根地址仍能进入超级智能体。
2. 两个产品参数地址可以直接打开。
3. 顶部按钮能在三个产品间往返切换。
4. 浏览器前进、后退能恢复产品状态。
5. 窄屏下切换按钮和菜单可用。
6. 执行格式检查、定向Lint和站点构建。

