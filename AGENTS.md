# Local Project Rules

## 三产品统一维护

开始修改本仓库前，先阅读：

1. 工作区当前生效的 `产品规划定义基线-V*.md`。
2. 本仓库的 `UNIFIED_PRODUCT_ENTRY.md`。

本仓库是超级智能体、智能体管理后台和记忆管理系统的统一原型仓库。后续建设管理后台或记忆管理系统时，必须接入当前站点和顶部产品切换器，不得另建割裂的原型地址、独立发布入口或外部跳转。

固定产品标识如下：

- `superagent`：默认根地址。
- `admin`：`?product=admin`。
- `memory-system`：`?product=memory-system`。

实现时遵守以下规则：

- 保留顶部产品切换按钮，并在三个产品页面中使用同一套切换组件和产品标识。
- 管理后台代码优先放入 `site/app/products/admin/`；记忆管理系统代码优先放入 `site/app/products/memory-system/`；跨产品外壳代码放入 `site/app/product-shell/`。
- 当前切换逻辑仍在 `site/app/fiscal-workbench.tsx`。允许后续拆分，但拆分后必须保持现有查询参数、直达地址和浏览器历史行为兼容。
- 三个产品共享站点入口，不共享产品职责。不得把超级智能体的用户工作功能、后台的组织治理功能和记忆系统的长期记忆治理混成同一套导航或对象。
- 不复制已构建的静态产物作为长期源码，不通过iframe或另一个本地端口拼接正式原型。
- 默认根地址不得改变；旧链接必须继续可用。
- 修改产品入口后，至少验证三个直达地址、菜单切换、前进／后退、窄屏布局、控制台、格式检查、定向Lint和构建。

